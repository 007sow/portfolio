import React from "react";
import { SpinningText } from "./spinning-text";

export function SpinningTextBasic({ className }) {
  return (
    <SpinningText className={className}>
      lets connect - lets connect -
    </SpinningText>
  );
} 